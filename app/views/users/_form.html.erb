<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br/>
    <%= f.text_field :name, :size => 30 %><br/>

    <%= f.label :password %><br/>
    <%= f.password_field :password, :size => 30 %><br/>

    <%= f.label :confirmation, 'Confirm' %><br/>
    <%= f.password_field :confirmation, :size => 30 %><br/>
    
    <%= f.label :rating %><br/>
    <%= f.number_field :rating %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
